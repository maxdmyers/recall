{
  "_comment": "Hook entries this repo expects in ~/.claude/settings.json. Manually merge into the existing hooks block; do not replace settings.json wholesale (it has other hooks, e.g. claude-code-monitor). The Stop entry was added when hooks/capture-session.sh was written; the SessionStart entry was added when hooks/inject-knowledge.sh was written.",
  "hooks": {
    "Stop": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "/Users/maxmyers/Sites/recall/hooks/capture-session.sh"
          }
        ]
      }
    ],
    "SessionStart": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "/Users/maxmyers/Sites/recall/hooks/inject-knowledge.sh"
          }
        ]
      }
    ]
  }
}
